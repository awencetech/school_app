# Copy to .env and fill values for local testing
PORT=8080
TARGET=https://coresg-normal.trae.ai
# Optional: set allowed origin for CORS (your dev server)
ALLOW_ORIGIN=http://localhost:8000
# Optional: API key to forward to upstream as Authorization: Bearer <API_KEY>
API_KEY=
